# macOS folder metadata
.DS_Store

# Claude Code local config (machine-specific paths, not for the repo)
.claude/

# Source asset for the OG image — only og-image.png is shipped
og-source.png

# Local working/handover docs
HANDOVER.md
El_cronista_de_beisbol_definitivo.md

# Secrets — API keys etc. Never commit these.
.env
*.env
tools/.env
